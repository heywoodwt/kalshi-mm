# Keep the build context small and free of local/runtime cruft.
target/
.git/
.github/
.env
*.log
deploy/
# models/ is NOT excluded — the committed production policies are baked
# into the image (see Dockerfile).
