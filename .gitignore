target/
*.log
# Machine-local build tuning (e.g. a target-dir override to dodge OneDrive
# file locks) — never portable, so it's per-checkout, not per-repo.
/.cargo/config.toml
# Runtime-only: production ONNX checkpoints copied here at deploy time
# (test fixtures live in tests/fixtures/models/ and ARE committed) and the
# local credentials file.
/models/
.env
